import { Routes } from '@angular/router';
import { DatabaseMigrationComponent } from './database-migration.component';

export default [
    {
        path: 'database-migration',
        component: DatabaseMigrationComponent,
    },
] as Routes;
